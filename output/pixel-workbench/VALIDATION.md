# 灯板工具重构验收记录

日期：2026-09-07。范围：本地正式工具 `website/tools/emoji2pixel/`、已确认的独立预览关闭按钮及两份设计规范。未提交、推送或部署；原有 AGENTS.md 修改未纳入本次工作。

## 实现

- 素材、画布与当前对象三区；窄屏使用素材／调整／动画切换，画布保留在可见区域。
- 普通选择替换当前图形，显式添加才追加一帧；支持替换、删除及 GIF 导入撤销。动画折叠保留帧。
- 区分整幅图形、选区、全局颜色处理与单帧覆盖。当前帧导出使用已编辑像素。
- 下载面板区分当前帧 PNG、效果预览、完整原始像素 GIF；发送面板展示真实目标及内容。
- 八语言同步。关闭按钮为无圆框 SVG 叉号，保留 44px 点击区域及键盘焦点。

## 实际浏览器验证

- 八语言分别检查 1440、768、390、320px，共 32 个布局组合：无横向页面溢出，无缺失的可见标签。
- 320px 下八语言的四个对话框共 32 个组合：无横向溢出。
- 中英文核心流程覆盖空白开始、Emoji 替换、真实图片导入、显式添加帧、切换与播放暂停、动画折叠、删除撤销、GIF 导入撤销、变换参数及单帧颜色覆盖。
- 矩形选区拖动、按颜色选择、容差、填充、复制／粘贴选区按钮及撤销已操作。相关 A、F、Delete、Cmd+Z、Escape 快捷键已检查。
- 下载真实文件并检查：原始 PNG 为 32×32，效果 PNG 为 640×640，GIF 为 32×32、12 帧，与测试时关键帧和过渡设置一致。复制完整图像成功反馈已检查。
- Tab 焦点可见，Escape 关闭后焦点返回打开按钮；关闭按钮计算样式无边框，打开时焦点落在标题。
- 最终页面控制台未发现 error 或 warn。

## 代码及资源检查

- `node --check`：正式 app.js、locales.js 和独立预览 preview.js 通过。
- `git diff --check` 通过。
- 原有控制 ID 均保留且无重复；八份 JSON 均包含 HTML 引用的翻译键；内嵌语言包与 JSON 一致；本地资源存在；许可文件未改；正式工具不依赖 design-preview。
- 本目录 behavior-check.cjs 的 9 项检查通过，覆盖替换撤销、单次追加、当前帧输出、GIF 撤销与时序、触控路由、Unicode 解析，以及发送请求链成功／上传失败／展示失败。

## 验证边界

- 未提供明确测试设备与发送授权，因此没有向真实设备发送。发送请求链通过受控假响应检查，不能代替设备端到端验证。
- 浏览器自动化环境无法切换系统减少动效偏好。使用 reduced-motion.html 验证夹具强制应用共享样式中的同一组减少动效声明，确认过渡时长为 0、滚动为 auto、面板仍可操作；未宣称验证了系统偏好切换。
- 自动化工具截获 Cmd+V，未能把该实际按键传递至页面，因此该快捷键未完成浏览器端验证；选区粘贴按钮已验证。

本文件及测试夹具属于本地验收材料，不是生产部署依赖。

## 发布记录

2026-09-07 用户另行授权提交、推送和部署后，已将正式工具及两份设计规范提交为 `7405d7b56c990ff7af55752c1c568bf01a8e569c`，推送到 GitHub main。Cloudflare Pages 由 github:push 自动部署，生产部署 ID 为 `0bb16550-4877-48c6-9dde-cc632d99c2d7`，API 显示 deploy success，GitHub Cloudflare Pages 检查 success；部署目录为 website。

正式地址 https://rminte.com/tools/emoji2pixel/ 已验证素材替换、显式添加两帧、播放暂停、PNG 下载反馈、发送预览及英文切换；页面控制台无 error/warn。线上 app.js、styles.css、locales.js 和 brand.css 与提交字节一致，HTML 仅有 Cloudflare 邮箱保护改写。真实设备发送仍未执行。其他工作区修改及本地讨论、预览、验收材料未提交。
