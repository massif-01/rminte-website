# AI 与贡献者工作约定

修改网站前先阅读 [DESIGN.md](DESIGN.md)。该文件回答“网站为什么这样设计、应当是什么样子”，本文件回答“如何修改并证明没有偏离”。

本文件使用 Codex 默认识别的 `AGENTS.md` 文件名，放置于仓库根目录。开始任何网站设计或界面修改前，必须读取同目录的 [DESIGN.md](DESIGN.md)，并将其中规范用于实现和验收。`DESIGN.md` 不属于 Codex 默认自动加载的指令文件；由本文件明确要求读取，不依赖用户每次提醒。

识别规则依据：[OpenAI 官方 AGENTS.md 文档](https://learn.chatgpt.com/docs/agent-configuration/agents-md)。

## 1. 工作原则

- 从需求与问题本质出发，每个设计和实现决策都要能解释“为什么”。不照搬模板，也不为追求新鲜感推翻已经确认的设计。
- 目标、动机或范围存在关键歧义时，先澄清；明确授权范围内的常规优化、修复和验证直接完成，不重复索取许可。
- 查明根因再修改。避免过度抽象、防御、兜底和无关重构。
- 调整视觉元素时，按完整信息组审视图片、参数、注释、分隔线、对齐和留白；尺寸或位置变化时同步处理其关联元素及背景关系，不只修改用户点名的一个属性。检查桌面、窄屏和动效状态，关联调整仍限于当次授权范围。
- 用户最新明确要求优先于本文。不要把本文件的通用规则用作阻止已授权工作的理由。
- 保留工作区已有修改。不得擅自重置、清理、暂存隐藏或覆盖其他贡献者的工作。

## 2. 开始前

1. 查看 `git status` 与本次涉及文件的差异，区分已有工作和本次修改。
2. 阅读 `DESIGN.md`、目标页面及其实际加载的 CSS、JavaScript、数据和媒体引用。
3. 对视觉或动效任务，在浏览器查看当前页面及相关交互。首页要滚动到目标章节，不能只看首屏截图便判断整个网站。
4. 明确此次要改善的问题、必须保留的体验和验收方式，然后实施。

正式站点源码为 `rm01-website-soft/`。`design-preview/`、`copy-review/`、`output/` 为本地方案、讨论或验证材料，不作为生产页面依赖；其他贡献者的检出中可能没有这些目录。

## 3. 设计边界

- 遵循黑／石墨灰／银白、白色矢量 Logo、统一字体与留白、胶囊文字按钮的既定规范。
- 保留首页视频入口、自动播放、结构分解、宝石点亮和工艺切换，不用静态排版替代。
- 不擅自调整宝石光线或遮罩。具体恢复值与图片选择见 `DESIGN.md`，避免反复复制参数产生不同版本。
- 图册、下载、指南必须与首页属于同一品牌体系；也要分别满足看图、找文件和阅读操作指南的需要。首页与图册采用纯黑，下载与指南采用较轻石墨灰；具体配色以 `DESIGN.md` 为准。灯板工具和 OTA 保留已确认的石墨灰，不因首页配色变化自动跟随修改。
- 合作商像素工具与独立服务只有在当次任务明确涉及时才修改。不得因它们位于仓库中或引用共享样式就扩大任务范围。
- 审美重构若改变色彩方向、主要布局、关键影像入口或叙事方式，先提交设计逻辑与独立示范供用户确认，再改生产页面。已确认方向中的常规实施与修复无需反复走预览审批。

## 4. 实现规则

| 修改内容 | 优先位置与要求 |
| --- | --- |
| 主站、OTA 与灯板工具统一配色 | `rm01-website-soft/assets/brand.css`，集中复用语义变量；其他独立服务按授权范围处理 |
| 共享字体、圆角与组件基础 | `rm01-website-soft/assets/brand.css`，注意独立服务也在复用 |
| 首页与共用基础 | `assets/styles.css`，修改实际生效规则 |
| 专用页面布局 | 对应的 `gallery.css`、`downloads.css`、`guides.css` |
| 首页数据与双语文案 | 检查 `assets/site-data.js` 与 HTML 的对应关系 |
| 主站页脚与联系面板 | 复用 `assets/styles.css`、`assets/site-data.js` 与支持页的 `assets/footer.js`；首页由 `main.js` 处理交互，同步检查各页面 HTML 与指南生成模板 |
| 行为 | 检查相应页面脚本，避免仅为视觉调整改写交互逻辑 |

- 共享样式改动要检查所有引用页面；独立服务若通过生成文件复用样式，也应识别其依赖，但不得顺带发布服务。
- 不增加新的样式框架、字体或复杂组件体系来解决局部视觉问题，除非需求确实需要。
- 清理被替代规则，避免持续追加覆盖文件、高优先级选择器与 `!important`。
- 保留语义标题、替代文本、可访问名称、可见焦点、键盘操作及减少动效支持。
- 文件链接与资源引用必须在实际部署目录下解析。不得依赖仓库外文件或本地预览服务特有路径。
- 视觉任务不自动授权改文案、规格、售价、承诺、版权或下载材料。明确获准改文案时，同步检查中英文含义及其所有展示位置。

## 5. 按改动范围验收

纯文档修改检查内容、链接及差异即可，不为此新增运行测试。页面修改必须有实际浏览器验证；一次截图或脚本语法通过不足以证明交互正常。

| 涉及范围 | 最低验证内容 |
| --- | --- |
| 共享样式 | 首页、图册、下载、指南入口及四篇指南；中英文；1440、768、390、320px 宽度 |
| 单页局部样式 | 目标页面的桌面和手机状态、中英文；受影响的共享组件 |
| 首页影像或布局 | 视频确实播放、滚动分解起止与中间状态、宝石明暗过程、工艺全部步骤、减少动效状态 |
| 散热图文 | 图、注释与短横线的整体比例；与背景旋风的位置关系；滚动隐藏／中间／完整状态及向上回退；窄屏取景与英文换行 |
| 图册 | 图片加载、横竖图完整展示、计数、前后切换、键盘与滑动；底部说明和翻页对齐 |
| 页脚与联系面板 | 页脚文案、版权符号、Logo、GitHub 与邮箱目标；中英文同步；开关、Escape、焦点及窄屏换行 |
| 下载 | 分类跳转、链接目标、资源可访问性及至少一个实际文件下载 |
| 指南 | 搜索、结果跳转、目录锚点、移动目录、复制结果、表格与代码滚动 |
| 导航与弹层 | 桌面与手机、开启与关闭、Escape、语言切换、焦点可见性 |

检查正文和标题是否溢出、固定导航是否遮挡内容、资源是否失败，以及控制台错误是否与本次修改有关。涉及 CSS 时比较计算后的样式与截图，不能只核对源码数值。

对改动的 JavaScript 运行语法检查，并检查差异中的空白错误。验证通过后不无理由扩大测试；存在新改动、失败或未解决疑点时再补充。

## 6. 交付与发布

- 用简洁中文说明改了什么、为什么、验证了什么，以及确实存在的未验证项。报告真实结果，不把预期效果当成已验证效果。
- 新的已确认设计规则发生变化时，同步更新根目录 `DESIGN.md` 与站点内的简版 `DESIGN.md`，并删除或改写被替代规则；归入对应章节，不在末尾不断追加重复条款。`AGENTS.md` 保留工作方法与验收要求，具体视觉参数放在设计规范。不要将尚在讨论的方案写成强制标准，也不要将本地实现写成已经上线。
- 提交或发布前检查最终差异，仅纳入本次授权文件。其他任务的未完成修改、本地讨论表、草稿与验证输出不得混入。
- 发布需有对应授权。用户已经明确要求部署或同步 GitHub 时，完成检查后继续执行，不额外增加确认环节。
- 发布前重新核实仓库、分支与部署配置，不沿用记忆中的远端状态。GitHub 权限通过实际 `gh api`、push 等结果判断，不单凭 `gh auth status`。
- 生产包来自确定的提交或快照，部署根为 `rm01-website-soft/`；核对本地资源完整性以及部署平台当时的限制。
- 发布完成必须检查正式地址上的页面、关键资源和核心交互，并核实 GitHub 提交与部署版本对应。只看到上传成功不能直接宣称上线验证完成。
