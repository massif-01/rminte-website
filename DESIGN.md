# RMinte 网站设计规范

本文件供设计者、贡献者和 AI 使用，定义官网应呈现的品牌气质、视觉规则及体验边界。执行流程见 [AGENTS.md](AGENTS.md)。

基准为 2026-09-05 用户确认并上线的深色官网版本（提交 `d2e287c`），以下规则已纳入截至 2026-09-06 的后续设计决定；文档中的当前规范不代表这些改动均已发布。实际实现位于 `rm01-website-soft/`；`design-preview/` 是本地探索材料，不是生产依赖，也不保证存在于其他贡献者的环境中。

## 1. 为什么这样设计

RM-01 是高价值的本地 AI 推理硬件。用户描述的价格定位接近 8,000 美元，这仅用于理解设计定位，不构成可自行发布的售价文案。

价值感来自真实产品、材料工艺、内部结构及运行能力。让摄影与动态影像承担主要表达，让界面帮助观众观看、理解和操作。黑色背景、克制的银白强调与有节奏的留白，应当突出这些证据。

设计决策必须回答三个问题：它是否更清楚地呈现产品价值？是否改善阅读或操作？是否与全站其他页面一致？仅仅“更流行”或“更像某个模板”不构成修改理由。

## 2. 适用范围与依据

- 官网首页、图册、下载中心、指南入口及指南正文遵循本规范。
- 合作商像素工具 `rm01-website-soft/tools/emoji2pixel/` 不自动纳入官网审美重构。只有任务明确涉及它时才修改。
- `services/` 下的独立服务按当次任务确定范围；复用品牌样式不等于获准改造服务界面或业务流程。
- 用户最新明确要求优先。已确认规则用于判断设计是否偏离，当前代码用于核对真实行为；出现矛盾时查清原因，不把偶然的代码差异直接当成新规范。
- 本文件为根目录的完整规范；站点内 [DESIGN.md](rm01-website-soft/DESIGN.md) 为简版。未来改变规范时同步维护两者，避免出现相互矛盾的要求。

## 3. 颜色与表面

产品首页和图册采用纯黑背景 `#000000` 与银白；指南、下载、灯板工具和 OTA 保留较轻的深石墨灰与银白。颜色、字体、圆角和基础组件集中在 `assets/brand.css`，复用语义变量，不在各页面重新发明近似颜色。

| 用途 | 变量 | 当前基准 |
| --- | --- | --- |
| 页面背景 | `--bg` | `#141618` |
| 次级背景 | `--bg-2` | `#191c1f` |
| 信息面板 | `--panel` | `#1c1f22` |
| 加强面板／悬停表面 | `--panel-strong` | `#24282c` |
| 主文字 | `--ink` | `#e5e7e8` |
| 正文辅助色 | `--ink-soft` | `#cbd0d4` |
| 说明文字 | `--muted` | `#a9adb2` |
| 次要元信息 | `--dim` | `#969da3` |
| 行动强调 | `--accent` | `#e8eaed` |
| 分隔线 | `--line` | `rgba(235,238,240,.13)` |
| 加强边界 | `--line-strong` | `rgba(235,238,240,.28)` |

### 按页面职责使用背景

用户于 2026-09-06 确认：产品首页所有章节（包括“世界级制造”）及图册的页面、影像容器背景使用纯黑 `#000000`。这两页在 HTML 根元素使用 `product-theme`，由 `assets/brand.css` 将 `--bg` 和 `--bg-2` 设为纯黑；信息面板与控件保留必要层次。

指南入口、四篇指南正文和下载中心保留石墨灰：页面背景 `#141618`、次级背景 `#191c1f`。灯板工具和 OTA 保持当前配色，本次首页与图册的调整不扩展至独立服务。指南正文直接位于页面背景上，不增加包裹整篇文章的大卡片。

页面背景配色调整本身不改变媒体内部颜色、宝石滤镜或滚动逻辑；散热取景与渐显等已确认调整见第 7 节。

尽量减少界面中的蓝紫色渐变、彩色光晕和装饰发光，避免形成泛化的 AI 产品视觉。真实照片和影片中的宝石颜色、材料反光不因此去色。错误、成功等功能状态可以使用有明确语义的颜色，同时配合文字或图标表达。

面板只用于分组、操作或阅读提示；下载列表优先采用对齐和细分隔线。不要把每段内容都包成卡片，也不要为了“高级”降低正文可读性。

## 4. Logo 与字体

- 深色页面使用 `assets/images/logo-white.svg`，保留原始矢量路径、比例和留白。不得拉伸、描边、加光晕或换成普通文字拼写。
- 首页左上角 Logo 在向下滚动的前 240px 内逐渐淡出，回到顶部恢复；完全隐藏后不接收点击和键盘焦点。其他菜单保持现有行为，图册与支持页 Logo 不变；减少动效模式下 Logo 保持可见。
- 界面字体使用 Geist / Noto Sans SC；产品字标通过现有 `.rm-mark` 使用 Quantify RM。不要将品牌字体扩展到普通正文。
- 使用现有字体加载方式和字重。标题一般采用 500–550 的中等字重，正文一般采用 400；产品字标保留其 700 字重。
- 中文和英文分别检查换行。标题可为语义节奏换行，但不能依靠固定高度、裁切或过小字号塞入布局。

| 层级 | 桌面基准 | 移动端基准 |
| --- | --- | --- |
| 子页面主标题 `--page-title` | 流体字号，最大 64px | 流体字号，通常从 34px 起 |
| 子页面区块标题 `--page-section-title` | 28–36px 流体字号 | 复用变量，按可读性检查 |
| 指南正文 h2 / h3 / h4 / h5 | 32 / 24 / 19 / 16px | 26 / 22 / 18 / 16px |
| 页面介绍与指南正文 | 16px | 15px |
| 正文行高 | 指南与主要介绍通常为 1.9 | 同左 |

图册为让照片成为主体，采用独立标题层级：桌面 36px、820px 及以下 32px，简介为 14px；不套用支持页的大标题。

首页具有专门的主视觉和章节标题层级，应保留当前已确认的比例与响应式规则，不把所有标题统一成同一字号。章节序号与短标签是辅助信息，不能抢过产品标题。

## 5. 布局与间距

- 内容最大宽度使用 `--section-w: 1280px`。
- 页面左右留白使用 `--section-x: clamp(24px, 5.55vw, 88px)`，小屏基准为 24px。
- 常规章节间距使用 `--section-space: clamp(88px, 8vw, 128px)`，主要内容间距复用 `--content-space: 48px`。
- 支持页面起始留白使用 `--page-top`，当前桌面 164px、较窄屏幕 124px；首页与图册使用各自已确认的布局。
- 优先沿用相邻组件的间距层级，保持同级元素一致；影像场景与长文阅读允许具有明确用途的差异。
- 固定导航必须保持可读，不与滚动文字重叠。锚点跳转后的标题不能被导航遮住。
- 指南、下载与图册使用贴顶、通栏且具有不透明背景色的导航元素，背景直接设在导航上，下缘渐隐独立处理，不用页面伪元素代替导航背景；导航控件按 `safe-area-inset-top` 下移，横屏内容避开左右安全区域，避免滚动内容露到状态栏背后。
- 移动端重新组织列与内容顺序，不简单缩小桌面画面。正文不产生整页横向滚动；宽表格和代码块在自身容器内滚动。

## 6. 按钮与交互控件

| 类型 | 形状与表现 |
| --- | --- |
| 文字按钮、主要行动、导航项、语言切换 | 胶囊形，`--radius-button: 999px` |
| 主要行动 | 银白底、深色字，清晰突出 |
| 次要行动 | 深色底、细边框，与主要行动区分 |
| 图册箭头、等宽等高图标按钮 | 圆形；常规点击区域以 44px 为基准 |
| 指南搜索容器 | 32px 圆角，内部按钮沿用胶囊形 |
| 信息面板 | `--radius-panel: 8px` |
| 表格容器、文件类型标识等 | `--radius-control: 4px` |

圆角根据控件用途确定，不把信息面板做成巨大胶囊，也不将文字按钮改回小圆角矩形。主要行动按钮通常高 48px，下载行动通常至少 44px；小型工具控件沿用自身已验证尺寸。

窄屏（不超过 1120px）顶部语言与菜单按钮统一为 56 × 44px 胶囊，背景与边框一致；菜单图标居中。主站、下载、指南与图册的语言按钮统一显示目标语言名称：英文入口为「EN」，中文入口为「中文」。

悬停反馈采用轻微背景、边框变化或位移。键盘焦点必须可见；菜单、弹层、语言切换、复制、搜索应保留现有行为与状态提示。

界面中的方向箭头、关闭、播放、编辑操作等图标使用 SVG，统一采用 24 × 24 坐标系、1.6 线宽和圆角端点，并按按钮尺寸居中。不要用字体字符代替操作图标；正文标点、尺寸与数学符号、键盘说明及用户选择的 Emoji 素材保留原义。已有 Logo 和菜单线条沿用矢量或 CSS 实现。

## 7. 摄影、视频与滚动叙事

这是品牌表达的核心，不能把它们视作可随意删除的装饰。

- 保留首页原有自动播放视频及其入口方式，保留静音、循环和加载行为。
- 保留硬件结构的滚动分解、蓝宝石随滚动点亮、工艺段落切换，以及已有产品视频。
- 蓝宝石底图与点亮层共同覆盖章节，保持相同取景并与点亮遮罩对齐；不再将图片元素整体缩小至 85%，避免手机右侧出现图片断边。
- 绒光玻璃保持既有图片缩放、位置和亮度，采用左侧 28% 渐隐接入黑色。连接接口保持取景，图片上下各 6% 渐隐，左侧过渡接入纯黑。换卡视频保留既有比例、位置和上下 8% 渐隐，外围过渡统一为纯黑；不为铺满章节而放大或裁掉展示重点。
- 不以静态卡片、平铺文字或新的叙事模板替代这些体验，除非用户明确确认该方向。
- 不擅自改变宝石光线、遮罩、时序或亮度。当前恢复后的亮层滤镜为 `saturate(0.92) brightness(1.2144) contrast(1.05)`；不要重新采用此前试验的 150% 亮度。该值是实现基准，不代表要求后续主动增强光效。
- 当前“6063 铝合金机身”采用图册 09：`assets/gallery/DSC00150.jpg`。
- 图册完整展示原始摄影，沿用 `object-fit: contain`，保留 11 张图片和切换方式；不强制裁掉产品或增加背景模糊复制层。
- 散热章节的风扇叶片放大图、分隔线与 0.13 mm 说明作为整体，随向下滚动渐显，向上滚动同步退回；背景风扇视频继续播放。减少动效模式下图文完整可见。叶片特写采用缩小前尺寸的 70%（原最大宽高为 424 × 374px），桌面图片高度为 `clamp(196px, 22.4vw, 261.8px)`、820px 及以下高度为 `196px`。图片、横线与注释共用紧凑容器（最大宽度 `296.8px`），统一靠右；761px 及以上整组落在真实风扇的右下旋风区域，完全显现时注释底部距场景下缘 `32px`；760px 及以下在正文之后顺排。窄屏背景取景保留右侧真实风扇。参数与说明字号保持不变。
- 尊重 `prefers-reduced-motion`；减少动效后，内容仍须完整可见且可访问。

首页制造精度章节保留向下滚动切换四组图文的方式，不增加点击或左右滑动切换。首页唯一页脚置于制造精度的粘性场景内，从第一组图文起随章节出现，不再等切换至最后一张；窄屏内容高于视口时允许自然滚动到完整落款，再保持其可见。右下方显示“向下滚动，查看更多工艺”（英文同步），最后一组隐藏提示，向上回滚恢复。减少动效或视口高度不足 740px 时沿用四组顺排，不显示切图提示。页脚细分隔线与联系交互保留。

首页工艺图文与内置页脚之间的间距为桌面 20px、窄屏 16px；场景本身不叠加底部留白，也不继承章节通用上下留白。

## 8. 页面职责

| 页面 | 设计重点 | 必须保留 |
| --- | --- | --- |
| 首页 | 动态产品展示、工程能力、材料与工艺的连续表达 | 视频入口、结构分解、宝石点亮、工艺切换、联系入口 |
| 图册 | 安静的深色界面，让完整摄影成为主体 | 图片顺序、计数、按钮、键盘及滑动切换 |
| 下载中心 | 类别、文件名称、说明和下载行动清晰对应 | 资源地址、文件属性、发布入口和真实下载行为 |
| 指南入口 | 角色入口等权、网络专题清晰、搜索易用 | 角色区别、搜索结果、语言切换和目标链接 |
| 指南正文 | 长文阅读、目录定位、表格与代码可用 | 内容层级、目录、复制、锚点、双语内容 |
| OTA 服务 | 地址复制、接入步骤与当前发布分层 | 设备侧升级、全部接口与发布资源、中英切换 |
| 灯板工具 | 素材／画布／属性工作区与响应式面板 | 八语言、图形与动画处理、导出和设备发送、原作者及许可 |

视觉调整不自动包含文案修改。不得因排版方便删除技术限定、改写参数、扩张产品承诺，或纠正未授权修改的版权文本。当前文案讨论材料位于本地 `copy-review/`，其中的建议不等于已批准的生产文案。

### 首页推理引擎与整机协同

2026-09-06 确认采用 A 版抽象磨砂方案。推理引擎使用三层软件／计算平台图与三段可切换说明；`RMinte Inference` 中的 RMinte 使用现有 Quantify RM 字标。整机协同桌面左侧为说明与四个功能标签，右侧为错位排列的模块关系图；手机重新排布模块与连线。

模块采用石墨灰内部结构、银灰边缘与磨砂表面。默认背景模糊 2.5px，功能聚焦、悬停或点击查看时降至 0px，过渡 450ms；减少动效时关闭过渡。ESP32、W5500、交换机与 MAC/PHY 使用不同的抽象内部结构，不模拟真实裸片或封装引脚。控制模块统一标为 ESP32，不锁定 S3 型号。两台计算机各有独立存储，ESP32 另有自己的配置存储。 应用侧独立存储位于 x86 左下方，连线从 x86 左侧折向存储；交换机说明贴近模块下缘，存储及其说明向上收紧。图内 CUDA／x86 字号为 16、计算机标签为 14、TianshanOS 为 17（手机为 20），均按图示坐标单位缩放；左侧章节标题不变。推理计算机、TianshanOS 与应用计算机标签到对应模块的可见间隙保持一致，桌面约 12、手机约 8 个图示坐标单位；按文字上下边缘衡量，不直接比较基线距离。

| 功能 | 清晰模块 | 强调连线 |
| --- | --- | --- |
| 配置驱动硬件 | ESP32 | 无 |
| 南北向设备控制 | W5500、RTL8367RB、两侧 MAC/PHY | W5500 与交换机、两侧 MAC/PHY 与交换机 |
| 安全配置包 | 配置存储 | 无 |
| OTA 与自动化 | ESP32、W5500、RTL8367RB | ESP32 → W5500 → RTL8367RB |

OTA 不强调计算机两侧 MAC/PHY 或其网络／PCIe 支路。切换功能会清除手动点击的查看状态。模块、文字与连线使用同一容器的归一化坐标，窗口缩放与断点切换后仍须对齐。每束三条连线沿各段法线偏移，端点贴合模块边缘；不使用遮罩隐藏穿越。图示表达内部连接关系，不表示真实 PCB 走线与器件布局。

硬件架构的五张模块卡片不显示 01–05 装饰性小序号，也不为序号预留标题空隙。首页各章节标签仅保留中英文名称，不显示 01–10 序号及其斜线分隔符。

### 图册

图册顶部标题、简介与 HDR 提示左对齐纵向排列。简介下方间隔 10px 放置银灰色 12px 提示与 Google Material Icons 的 HDR 矢量图标；图标使用银白色 `--ink`，正文使用灰色 `--dim`，垂直居中，不加光晕或闪烁。该标识说明图片支持 HDR，不表示当前设备已启用 HDR。中英文同步，手机自然换行。提示为“本图册支持 HDR 浏览，兼容设备可呈现更丰富的明暗细节。”

图册采用顶部标题与简介、居中完整摄影、底部单行图片说明与计数／翻页按钮的布局；手机将说明与翻页分行。移除图册内容区的装饰分隔线、重复编号、英文分类与分段进度条，保留 11 张照片、前后按钮、键盘及滑动切换。图册页底复用主站页脚、版权数据与联系脚本。

### 下载与指南

下载中心的分类入口、章节标题与发布条目不使用序号或重复的英文装饰标签；名称随中英文切换，依靠字号、留白和分隔线组织层级。文件格式、产品名称及版本信息保留。

指南入口不显示分组数量或卡片序号、英文装饰小标题，保留随语言切换的名称与章节信息。

### 共用页脚与联系面板

图册、下载、指南入口及指南正文与首页共用页脚：页脚上方内边距 24px、下方 40px；支持页正文末尾留白桌面 20px、窄屏 16px，不叠加章节级大间距。

主站八页均显示白色 Logo、公司名称、以「© 2026」开头的版权信息、GitHub 与联系入口。双语文案复用 `assets/site-data.js`；支持页联系交互复用 `assets/footer.js`，首页由 `assets/main.js` 处理。保持页面 HTML 与指南生成模板一致。

联系弹层采用单层石墨灰面板与细边框，标题只显示当前语言的“联系我们”；销售与渠道合作、制造与供应链合作、售前与售后服务三个邮箱按此顺序排列，使用细线分隔的文字入口及 SVG 斜箭头，不使用双层套框或大胶囊。关闭按钮为 44px 圆形，桌面弹层最大宽度 400px，手机两侧留白 16px。

### OTA 与灯板工具

OTA 展示文案中的 RMinte、RM-01 与 TianShanOS 使用现有 Quantify RM 专属字体；文件名、接口地址维持等宽字体。页脚 GitHub 使用与首页相同的猫图标及 GitHub 文字。

OTA 与灯板工具的语言、菜单控件沿用主站 44px 高胶囊样式与双横线菜单图标；菜单和语言按钮宽 56px，灯板工具的八语言下拉框按语言名称适当加宽。

## 9. 样式归属与验收

统一配色、字体、圆角和服务组件基础位于 `assets/brand.css`；主站共用布局与首页样式位于 `assets/styles.css`；图册、下载、指南布局分别位于 `gallery.css`、`downloads.css`、`guides.css`。页面先加载品牌基础，再加载共用与专用样式。

新增组件先复用现有规则。修复样式应找到实际生效的声明，不反复添加覆盖层、页面专用近似变量或 `!important`。原 `detail-refinements.css` 已合并，不重新引入。

验收应能证明：同级内容视觉一致，桌面与手机均可读，中英文不溢出，图片与视频正常，关键交互没有退化。具体检查流程见 [AGENTS.md](AGENTS.md)。
