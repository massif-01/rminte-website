# RMinte 官网｜模型页面内容与实现说明

> 版本：V1.0\
> 整理日期：2026-09-07\
> 页面名称：模型\
> 对应产品：RMQ3x、RMQ4\
> 用途：RMinte 官网独立「模型」页面的文案与项目实现依据。

---

## 一、页面定位与实现边界

### 1. 页面位置

在网站顶层导航中新增独立的 **「模型」** 标签页，承载 RMQ 系列的完整介绍。

- 这是独立页面，不是现有首页中的新增板块。
- 不与 RM-01 产品介绍混排，不以硬件产品作为这张页面的叙事主体。
- 页面可使用 `/models` 作为路由；最终路径遵循现有项目的路由规范。
- 本文件不指定前端框架、组件库或重新设计全站视觉体系；实现时沿用现有官网的设计语言。

### 2. 已确认的表达方式

公开页面统一使用 **「针对性训练」**，简明说明训练目标：让案件、科研与社会问题得到更深入的讨论与分析。

不单独设置训练理念、主题敏感或减少拒答的论述板块，也不以「后训练」「去对齐」「过度拒答」作为宣传标题或主要卖点。专业能力由真实测试集与对比图表展示。

场景卡片只需说清楚：**处理什么材料，完成什么工作。**

保留推理引擎与架构优化介绍。推理实现包括两条路线：自主开发的独立 C++ 推理引擎，以及基于 vLLM 定制改造的推理实现。

不单列「本地部署」板块，也不把本地部署作为本页反复强调的卖点。

### 3. 页面顺序

```text
模型 / Models
├── 01 首屏：RMQ 系列定位
├── 02 模型介绍：RMQ3x / RMQ4
├── 03 模型评测：真实测试与对比图表
├── 04 专业应用场景：九张场景卡片
├── 05 推理引擎：独立 C++ / 基于 vLLM 定制
├── 06 架构优化：四个优化方向
└── 07 收尾文案
```

下文第二部分给出公开页面文案，字段标签用于标识文案位置，不必照搬为页面文字；HTML 注释是内部占位说明。第三部分及之后为内部实现说明，不应直接整体渲染到官网。

---

## 二、正式页面文案

### 01｜首屏

**栏目名称**

RMQ 系列模型

**主标题**

> 为深入专业问题而训练，\
> 为高效推理而优化。

**介绍正文**

RMQ3x 与 RMQ4，是 RMinte 面向专业任务打造的模型。通过针对性训练，让案件、科研与社会问题得到更深入的讨论与分析。围绕不同模型架构，协同推进模型训练、推理引擎与底层计算优化。

**模型标识**

- **RMQ3x · 27B Dense**
- **RMQ4 · 125B MoE¹**

**按钮**

- 探索模型
- 查看评测

**参数注释**

¹ RMQ4 的 125B 指主干参数；完整参数组成与模块配置见详细规格。

---

### 02｜模型介绍

<!-- 实现说明：采用两张并列卡片，分别介绍 RMQ3x 与 RMQ4。 -->

#### RMQ3x

**规格摘要**

27B 参数 · 稠密架构

**卡片正文**

面向案卷、研究文献与复杂文本的理解、比对和分析。通过针对性训练深入专业任务，并围绕混合注意力与稠密前馈计算，开展专门的推理适配与优化。

**技术信息**

基于 Qwen3.6-27B · Dense · 混合注意力

#### RMQ4

**规格摘要**

125B 主干参数 · 稀疏多专家架构

**卡片正文**

面向复杂材料、多步骤分析与专业研究任务。通过针对性训练拓展专业应用，并围绕稀疏专家、混合注意力及相关计算结构，开展模型级推理优化。

**技术信息**

基于 Qwen3.8-Flash-Next · Sparse MoE · 混合注意力

---

### 03｜模型评测

**主标题**

> 专业任务，实测表现。

**介绍正文**

通过公开基准与专业任务测试，展示 RMQ 系列在材料理解、证据分析、科学推理与复杂任务中的实际表现。

**图表分组名称**

- 专业任务
- 公开基准

<!-- 图表占位：接入实际完成的测试结果。此注释不展示为页面正文，不使用虚构分数、示意排名或未经测试的提升幅度。 -->

---

### 04｜专业应用场景

**主标题**

> 从实际材料，进入专业工作。

**介绍正文**

面向案件、安全、科研与社会研究中的具体任务，围绕材料开展理解、比较、分析与论证。

#### 司法与法律服务

围绕卷宗、询问笔录、鉴定意见与法律文书，梳理事件时间线、比较不同陈述、整理争议焦点，辅助分析证据与论证之间的关系。

#### 刑事调查与数字取证

围绕调查记录、通信材料与数字证据，关联事件和线索、比较不同解释、整理待核实问题，辅助形成可追溯的分析记录。

#### 网络安全防御

分析安全日志、漏洞报告、恶意代码与入侵痕迹，辅助开展告警研判、攻击链梳理、漏洞影响评估与安全事件复盘。

#### 平台内容治理

围绕用户内容、上下文对话、举报与申诉材料，辅助理解语境、对照治理规则、分析风险与整理判断依据，支持审核和人工复核。

#### 企业调查与反舞弊

比对举报材料、内部邮件、合同与交易记录，梳理事实关系、识别陈述差异、整理疑点，支持内部调查与反舞弊工作。

#### 法医、毒理与事故调查

围绕检验报告、毒理资料、事故记录与专业文献，比较不同机制解释，整理支撑材料，辅助分析伤害与事故的发生过程。

#### 材料与工业化学研究

分析研究文献、实验记录、检测报告与工艺资料，围绕材料性能、反应机理、腐蚀老化、失效原因和工艺偏差开展研究讨论。

#### 生命科学与生物医学研究

围绕生物机制、疾病研究与实验资料，比较文献结论、整合研究证据、讨论科学假设，辅助解释实验结果。

#### 社会学、犯罪学与伦理研究

分析访谈、案例与研究文献，比较理论、梳理观点和论证前提，支持社会现象研究与不同伦理框架下的讨论。

---

### 05｜推理引擎

**主标题**

> 不只训练模型，也优化模型如何运行。

**介绍正文**

RMinte 围绕 RMQ 系列构建两条推理实现：自主开发的独立 C++ 推理引擎，以及基于 vLLM 定制改造的推理实现。结合模型结构与实际任务负载，推进从模型适配、算子执行到运行时管理的协同优化。

#### 独立 C++ 推理引擎

由 RMinte 自主开发，以独立 C++ 运行时承载模型推理。围绕 RMQ 系列的计算路径开展专门适配，让模型结构、算子执行与资源管理紧密配合。

#### 基于 vLLM 的定制推理实现

在 vLLM 基础上进行定制改造，结合 RMQ 系列的模型结构与实际负载，推进模型执行、算子集成与运行配置优化。

<!-- 性能图表占位：完成同条件测试后，展示两条推理实现的延迟、吞吐和资源占用数据。没有实测结果时不展示虚构图表，也不写入未经验证的加速倍数。 -->

---

### 06｜架构优化

**栏目名称**

架构优化

**主标题**

> 面向模型结构，深入计算细节。

**介绍正文**

不同的模型，有不同的计算与数据访问方式。RMinte 将围绕 RMQ3x 与 RMQ4 的具体架构，开展关键算子优化、计算融合与访存优化，减少执行过程中的额外开销，让推理实现更贴合模型本身。

#### 混合注意力优化

**适用模型：RMQ3x · RMQ4**

围绕 Gated DeltaNet 的门控与状态更新，以及两款模型各自的注意力计算路径，分别研究输入预填充与逐 token 生成阶段的执行方式。面向 RMQ4 的 QSA，进一步关注块级索引、稀疏选择与相关数据访问。

#### 稠密计算与算子融合

**适用模型：RMQ3x**

围绕稠密前馈网络与投影计算，开展矩阵运算优化，并在适合的执行边界融合归一化、激活与残差等计算，减少中间结果读写和分散调用。

#### Fused MoE 专家计算

**适用模型：RMQ4**

围绕专家路由、token 排布、分组矩阵运算、共享专家与输出汇总，开展专门优化。在适合的环节引入计算融合，减少专家执行过程中的调度与数据搬运开销。

#### 门控残差与嵌入访存

**适用模型：RMQ4**

围绕门控残差的读写与归一化计算，以及 N-gram 嵌入的查表和数据访问，评估计算融合、预取与存储调度方案，使新增结构与整体推理过程协同运行。

---

### 07｜收尾

> 从针对性训练，到推理引擎，再到底层算子，RMinte 围绕 RMQ 系列持续推进模型与执行的协同优化。专业能力由任务检验，运行效率由实测呈现。

---

## 三、项目实现说明（内部使用）

### 1. 导航与页内交互

| 项目 | 实现要求 |
|---|---|
| 顶层导航 | 新增「模型」入口，与现有栏目并列；不将其作为 RM-01 页面子区块 |
| 独立路由 | 可使用 `/models`，遵循现有项目约定 |
| 首屏「探索模型」 | 跳转至本页模型介绍区，可使用 `#models` |
| 首屏「查看评测」 | 跳转至本页模型评测区，可使用 `#benchmarks` |
| 模型规格注释 | RMQ4 参数注释应可从首屏或模型卡片访问，不仅藏在内部文档中 |
| 详细规格 | 在本页使用展开区或其他符合现有设计的呈现方式；字段以最终交付配置为准 |
| 场景卡片 | 九个独立条目；不默认添加尚未存在的行业详情页面或空链接 |
| 引擎介绍 | 两条并列推理实现，不画成 C++ → vLLM 的串联流水线 |
| 架构优化 | 明确适用模型与实际完成状态，不把所有规划写成已上线功能 |

此处的路由、锚点及展示形式是实现用默认值，不要求改变已存在的工程约定。

### 2. 能力评测与性能评测分开

**模型能力图表**用于展示训练后的任务表现，放在「模型评测」板块。

**推理性能图表**用于展示引擎与执行效率，放在「推理引擎」板块。

不要把两者合并成不透明的「综合提升」分数。对照基座、其他模型和不同推理实现，需要明确各自的比较对象。

#### 模型能力图表需保留的信息

| 信息 | 要求 |
|---|---|
| 被测模型 | 名称及准确版本，区分 RMQ、原始基座与其他对照模型 |
| 测试集 | 名称、版本、样本量，以及公开基准或内部专业任务的属性 |
| 评分方式 | 指标名称、单位、越高越好或越低越好；人工评审时注明方法 |
| 测试条件 | 提示方式、思考模式、推理预算、检索资料与工具权限 |
| 执行记录 | 测试日期、评测脚本或可供核验的方法说明 |
| 结论范围 | 仅对测得的任务和条件作出结论，不由局部成绩推导全面领先 |

#### 推理性能图表需保留的信息

| 信息 | 要求 |
|---|---|
| 模型与权重 | 同条件对比使用相同模型权重；不同模型的测试不得冒充引擎对照 |
| 精度与量化 | 明确精度、量化方式和配置 |
| 硬件 | GPU/CPU、数量、内存或显存及相关运行条件 |
| 工作负载 | 输入长度、输出长度、批大小、并发度和缓存条件 |
| 软件 | 引擎版本、关键配置及测试日期 |
| 指标 | 可分别展示首 token 延迟、生成速度、吞吐、内存或显存占用 |

不能用单请求生成速度替代高并发吞吐，也不能仅凭实现语言宣称 C++ 一定优于另一条推理实现。

### 3. 暂无数据时的处理

当前没有提供可直接发布的评测数据。本文件不包含任何可被当作成绩发布的数字。

开发预览可以保留明确标注的占位区域；公开页面在数据未准备好时，应隐藏未完成的图表或清楚标注「评测结果待发布」。若隐藏整个评测区域，同步隐藏或调整首屏「查看评测」按钮，避免失效跳转。

模型的发布状态、下载方式、访问申请和授权方式尚未在本次讨论中确定，不应自行增加「立即下载」「开源可用」「商用免费」等按钮或承诺。

### 4. 两条推理实现的归属表达

独立 C++ 引擎可以写「自主开发」。

基于 vLLM 的实现应明确其上游基础，写「基于 vLLM 定制改造」，不把 vLLM 既有能力全部写成 RMinte 独创。

每项优化分别核对其在两条推理实现中的适用范围，不默认两条路线已经同时具备全部功能。能力或性能对比必须标明被测的具体实现。

### 5. 优化工作状态

本版将新增架构与算子优化按「规划方向」表述；这与独立 C++ 引擎已经开发是不同层面的事实。

| 优化项 | 对应模型 | 本版表述口径 |
|---|---|---|
| Gated DeltaNet 与混合注意力执行优化 | RMQ3x、RMQ4 | 规划方向 |
| 稠密前馈、投影与相关算子融合 | RMQ3x | 规划方向 |
| 专家路由、分组计算与 Fused MoE | RMQ4 | 规划方向 |
| QSA 索引、稀疏选择与相关数据访问 | RMQ4 | 规划方向 |
| 门控残差与 N-gram 嵌入访存 | RMQ4 | 规划方向；以最终保留结构为前提 |

完成具体适配并取得测试记录后，再把对应项目修改为「已支持」或「已验证」，并注明引擎、硬件与配置范围。不要因为基座或上游框架支持某项能力，就自动把它标成 RMQ 交付版本已支持。

---

## 四、技术口径与规格记录（内部使用）

本节保留上一轮讨论采用的基座架构信息，作为页面实现与后续核对的依据。这些信息不自动等同于 RMQ 最终交付配置；发布时应以实际模型产物、配置文件及保留模块为准。本次文档整理不新增评测或发布验证结论。

### 1. 基座对应关系

| RMinte 模型 | 讨论中采用的基座 | 架构信息 |
|---|---|---|
| RMQ3x | Qwen3.6-27B | 27B 稠密模型；64 层；Gated DeltaNet 与 Gated Attention 混合结构，每组三层 Gated DeltaNet、一层 Gated Attention；配合稠密前馈网络 |
| RMQ4 | Qwen3.8-Flash-Next | 48 层；Gated DeltaNet 与 Qwen Sparse Attention（QSA）混合结构；稀疏 MoE；Gated Residual 与 N-gram Embedding；512 个路由专家，每 token 激活 10 个路由专家，另有一个共享专家 |

### 2. RMQ4 的参数口径

上一轮讨论采用的 Flash-Next 基座参数说明为：

- 125B 主干参数。
- 约 6B 每 token 激活参数，具体统计范围应沿用并注明官方口径。
- 另有 51B N-gram Embedding。
- 另有 4B MTP 模块。

因此，正式页面不无条件写成「RMQ4：125B 总参数」。本版主卡片使用 **「125B 主干参数」**，首屏短标识通过参数注释说明口径。

以下字段必须根据 RMQ4 的实际产物确认后再发布：完整参数组成、激活参数范围、N-gram 嵌入是否保留、MTP 是否保留，以及实际专家配置。不能因为基座带有 MTP，就在页面增加已经实现 MTP 推测解码的承诺。

### 3. 为什么技术板块叫「架构优化」

两款模型的优化工作不只涉及矩阵乘法，也不只涉及 Fused MoE。

RMQ3x 虽然是稠密模型，但包含 Gated DeltaNet，其门控和递归状态更新需要单独考虑。RMQ4 除稀疏专家计算外，还包含 QSA、门控残差和 N-gram 嵌入相关的数据访问路径。

因此，技术范围包括算子执行、计算融合、访存与运行时管理。预取、存储调度和嵌入查表优化不应全部笼统称为「算子融合」。

### 4. Fused MoE 的准确表达

公开文案使用 **「Fused MoE 专家计算」** 或 **「专家计算融合」**。

这里指专家执行过程中相关计算与数据处理步骤的优化和融合，不是把多个专家的权重合并成一个专家，也不意味着路由到汇总的全部流程必然在单个内核内完成。

vLLM 已有 Fused MoE 与其他融合实现。RMinte 的技术介绍应落在自身实际完成的模型适配、执行改造和优化结果上，不把上游已有功能本身包装为独占能力。

---

## 五、参考资料（内部核对）

以下链接沿用已确认方案中的技术参考，供开发与发布核对。正文不需要复制整段技术资料，也不需要把内部参考清单直接展示为宣传内容。

### 基座模型

- Qwen3.6-27B 模型卡：<https://huggingface.co/Qwen/Qwen3.6-27B>
- Qwen3.8-Flash-Next 模型卡：<https://huggingface.co/Qwen/Qwen3.8-Flash-Next>

### 推理与算子实现

- Qwen3.8-Flash-Next 的 vLLM 部署说明：<https://recipes.vllm.ai/Qwen/Qwen3.8-Flash-Next>
- vLLM Gated DeltaNet 融合门控相关实现：<https://docs.vllm.ai/en/v0.24.0/api/vllm/model_executor/layers/fla/ops/fused_sigmoid_gating/>
- vLLM MoE 内核功能：<https://docs.vllm.ai/en/latest/design/moe_kernel_features/>
- vLLM 融合设计：<https://docs.vllm.ai/en/stable/design/fusions/>
- vLLM 模块化 Fused MoE 内核设计：<https://docs.vllm.ai/en/latest/design/fused_moe_modular_kernel/>

---

## 六、上线前检查清单（内部使用）

- [ ] 顶层导航存在独立「模型」入口，页面与 RM-01 介绍分开。
- [ ] 未在现有首页另加本次讨论中已取消的模型板块。
- [ ] 正式文案统一使用「针对性训练」，没有另起训练理念或拒答说明的大标题。
- [ ] 没有单独增加本地部署板块。
- [ ] 两款模型名称分别为 RMQ3x、RMQ4，大小写一致。
- [ ] RMQ4 的 125B 标明为主干参数，最终模块和完整参数口径已核对。
- [ ] 九张场景卡片齐全，正文均说明具体材料与具体工作。
- [ ] C++ 自研引擎与基于 vLLM 的定制实现并列且归属准确。
- [ ] 优化状态和适用引擎准确，没有把规划或上游能力写成已完成成果。
- [ ] 模型能力评测与推理性能评测分开呈现。
- [ ] 所有成绩来自真实测试，条件和指标可核验。
- [ ] 未完成的图表没有作为真实结果发布，按钮和锚点均可用。
- [ ] 未擅自增加开源、授权、下载、发布状态或性能承诺。
- [ ] 内部实现说明、技术核对记录与检查清单没有整体渲染到公开页面。

---

**页面核心表达**

> 为深入专业问题而训练，为高效推理而优化。\
> 专业能力由任务检验，运行效率由实测呈现。
