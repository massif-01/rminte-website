# RM-01 ネットワーク設定ガイド

## 1. 2 つの接続経路を理解する

RM-01 には USB-C を使う 2 つのネットワーク接続経路があり、本ガイドでは **C1 ポート**と **C3 ポート**と呼びます。どちらもネットワーク通信に使えますが、接続する機器とネットワーク構成が異なります。

| ポート | 主な用途 | 接続後のネットワーク構成 |
| --- | --- | --- |
| C1 | パソコン、タブレット、スマートフォンなどを RM-01 に接続 | 接続機器が RM-01 内部のスイッチネットワークに参加し、内部ノードになります |
| C3 | RM-01 を既存の LAN に接続 | LPMU が内部ノードのルーティングを担い、システム全体をスイッチやルーターのある上位ネットワークに接続します |

## 2. C1 と C3 の位置を確認する

### C1 ポート

C1 は本体側面の後方にあります。縦に並ぶ 2 つの USB-C ポートのうち上側が C1 です。ポートの下に刻印された接続図で位置を確認できます。

### C3 ポート

C3 は本体上部にあります。マグネット式カバーを開くと USB-C ポートが見えます。上部の共有 USB ポートで、TianshanOS から接続先を ESP、AGX、LPMU に切り替えられます。

## 3. C1 で手元の機器を RM-01 に接続する

データ転送対応の USB-C ケーブルで、iPad、パソコン、スマートフォンなどを C1 に接続します。RM-01 が DHCP でアドレスを自動割り当てするため、IP アドレス、ゲートウェイ、DNS の手動入力は不要です。

接続機器には通常、`AX88179` または USB Ethernet という有線ネットワークインターフェースが認識されます。その機器は RM-01 内部のスイッチネットワークに参加し、内部の各機器と通信できるようになります。

> C1 は接続機器を RM-01 の内部ネットワークに直接参加させます。信頼できる機器だけを接続してください。公共の場所や信頼できない環境では、管理されていない機器を C1 に接続しないでください。

## 4. インターネット接続を維持し、RM-01 と共有する

### 接続機器のインターネット接続への影響

<!-- operational-note -->

OS によっては、C1 接続後に `AX88179` を優先的なネットワーク出口として使うことがあります。C1 は主に RM-01 内部へのアクセス用であり、直接インターネットを提供するものではないため、既存のインターネット接続が一時的に使えなくなることがあります。

Wi-Fi、Ethernet などのインターネット接続を引き続き使う場合は、その優先順位を `AX88179` より高くしてください。

| OS | 設定項目 | 設定の考え方 |
| --- | --- | --- |
| macOS | サービスの順序（Service Order） | インターネットに接続する Wi-Fi または Ethernet を `AX88179` より上にします |
| Windows | インターフェースメトリック（Interface Metric） | 小さい値ほど優先されます。インターネット側の値を `AX88179` より小さくします |
| Linux | ルートメトリック（Route Metric） | 小さい値ほど優先されます。インターネットのデフォルトルートの値を `AX88179` より小さくします |

### RM-01 ネットワーク接続ツールを使う

手元の機器のインターネット接続を RM-01 に共有する場合は、サイトのダウンロードページから **RM-01 ネットワーク接続ツール**を入手してください。接続機器と C1 間の DHCP、ルーティング、共有ルールを設定し、インターネットのトラフィックを RM-01 内部ネットワークへ転送できるようにします。

現在の対応 OS は次のとおりです。

- macOS
- Linux
- Windows Pro

Windows Home は現在対応していません。

> このツールは接続機器のネットワークアドレス、ルート、共有設定を変更します。実行中に既存の接続が一時的に切れる場合があります。ダウンロード、リモートセッションなど、ネットワークに依存する作業を保存してから開始してください。

## 5. AI で C1 のネットワーク共有を設定する

Codex、Hermes など、ローカルでコマンドを実行できる Agent Harness は、自然言語の指示から構成を調べ、手元のパソコンから RM-01 内部への転送・共有設定を支援できます。対象パソコンでシステムコマンドを実行できることが必要です。通常の対話型 AI は手順を提案するだけで、設定そのものは実行できません。

### AI Agent を実行する場所

C1 に接続したパソコン上で直接実行することをお勧めします。インターネット出口、デフォルトルート、共有ルールを管理するのはそのパソコンであり、設定にはその管理者権限が必要です。

Codex、Hermes などを RM-01 内部で動かす場合は、手元のパソコンへのリモートコマンド実行権限と管理者の許可を事前に用意する必要があります。それがなければ、パソコンの共有、ルート、ファイアウォール設定は変更できません。

### 実行前の確認

- パソコンが C1 に接続され、`AX88179` または USB Ethernet が見えていること。
- パソコンの C1 アドレスが `10.10.99.100/24`、RM-01 内部ノードが引き続き `10.10.99.99`、`10.10.99.98`、`10.10.99.97` を使っていること。
- パソコンが Wi-Fi、Ethernet などでインターネットに接続できていること。
- Agent Harness がパソコン上でコマンドを実行でき、必要に応じて管理者権限を要求できること。
- ダウンロード、リモートセッションなどを保存済みで、ローカルでネットワーク設定を復旧できること。

### Agent にそのまま渡せるプロンプト

次のプロンプトは出荷時の構成を維持し、確認してから変更し、検証と元に戻す方法を示すよう求めるものです。コマンド実行に対応する Codex、Hermes などにコピーして使えます。

```text
RM-01 の C1 ネットワーク共有設定を手伝ってください。まず OS、ネットワークインターフェース、アドレス、デフォルトルート、ファイアウォールを調べ、以下の構成と一致することを確認してから変更してください。インターフェース名だけで推測せず、RM-01 内部ノードの固定アドレスは変更しないでください。

現在の構成：
- パソコンは USB-C で RM-01 の C1 に接続しています。通常は AX88179 または USB Ethernet と表示され、アドレスは 10.10.99.100/24 です。
- RM-01 内部ネットワークは 10.10.99.0/24 です。
- アプリケーションコンピューター（LPMU）は 10.10.99.99 です。
- 推論コンピューター（AGX）は 10.10.99.98 です。
- アウトオブバンド管理コンピューターは 10.10.99.97 です。
- パソコンは別のインターフェースでインターネットに接続しています。

目標：パソコンの既存のインターネット接続を保ち、それを C1 の AX88179 / USB Ethernet 経由で内部ネットワークに共有し、10.10.99.99、10.10.99.98、10.10.99.97 がインターネットにアクセスできるようにしてください。

制約：
1. OS、実際のインターネット出口、C1 のインターフェースを特定し、変更予定の項目を説明してください。
2. OS 標準の方法で変更を最小限に抑え、必要な IP 転送、NAT、ルートまたは Internet Connection Sharing を設定してください。
3. 既存のインターネット側インターフェースの優先順位とデフォルトルート、上記ノードのアドレスは変更しないでください。無関係なファイアウォールルールを消去・上書きしないでください。
4. 管理者権限が必要なら用途を説明してから要求してください。現在の OS または権限で安全に設定できなければ、停止して理由を説明してください。
5. 完了後はパソコンのインターネット接続、10.10.99.99、10.10.99.98、10.10.99.97 への到達性、内部ノードからのパブリック IP への接続と名前解決を検証してください。
6. 最後に、実行したコマンドまたは変更点、検証結果、元の設定に戻す手順を示してください。
```

macOS は通常「インターネット共有」と OS のルーティング、Linux は IP 転送と nftables または iptables、Windows Pro は Internet Connection Sharing（ICS）を使います。具体的なコマンドとインターフェース名は、Agent が実機で調べた結果に従ってください。Windows Home は現在対応していません。

### 設定の成功を確認する

完了後は、次のすべてを満たすことを確認します。

1. パソコンの既存のインターネット接続が使える。
2. パソコンから `10.10.99.99`、`10.10.99.98`、`10.10.99.97` にアクセスできる。
3. RM-01 内部ノードがパブリック IP にアクセスでき、ドメイン名を解決して接続できる。
4. Agent が単に成功と報告するだけでなく、実際の変更点と実行可能な復旧手順を示している。

### セキュリティ上の注意

> 管理者または root 権限を許可すると、Agent はルート、IP 転送、NAT、Internet Connection Sharing、ファイアウォールを変更できます。信頼できる Agent Harness のみを使い、許可前に操作計画を確認してください。パソコンがリモート接続などの重要な通信を担う場合は、ローカルで復旧できることを先に確認してください。

## 6. C3 で RM-01 を上位ネットワークに接続する

「上位ネットワーク」とは、スイッチやルーターが接続されている既存の LAN を指します。

### アダプターを用意する

次の機器を用意してください。

- USB-C ↔ RJ45 Ethernet アダプター 1 個
- スイッチまたはルーターに接続する RJ45 ケーブル 1 本

USB-C 側を C3 に、RJ45 ケーブルをスイッチまたはルーターに接続します。物理接続だけでは DHCP とルートの設定は完了しません。続けて RM-01 内蔵のネットワーク設定プログラムを実行する必要があります。

### 上部 USB を LPMU に切り替える

1. TianshanOS のホームページを開きます。
2. USB 切り替え（「USB 切换」）を探します。
3. 「USB」ボタンで上部 USB ポートの接続先を `LPMU` に切り替えます。
4. 現在の接続先が `LPMU` と表示されたことを確認してから続行します。

<!-- operational-note -->

切り替え時に現在の接続が一時的に切れることがあります。データ転送中に切り替えないでください。

## 7. LPMU 経由でシステム全体を接続する

出荷時の LPMU にはネットワーク自動設定プロジェクトが用意されています。LPMU が上位ネットワークに接続すると、内部ルーティングと NAT で AGX などのトラフィックを転送し、システム全体を接続します。

### プロジェクトの場所と必要な操作

プロジェクトは **LPMU の現在のユーザーのホームディレクトリ**の `~/network-setup/` にあり、LPMU 用スクリプトは `~/network-setup/lpmu/` にあります。

出荷時の機器では再ダウンロードや、README の導入担当者向け `scp`、`chmod`、インストール手順の再実行は不要です。通常は TianshanOS から設定を実行します。プロジェクト内のスクリプトやログを調べるのは、障害調査や追加開発のときだけです。

> `~/network-setup/` を不用意に移動、改名、削除しないでください。TianshanOS のネットワーク設定は、出荷時のパスとスクリプトに依存しています。

### 自動設定で行う処理

LPMU 上で順に次を実行します。

1. ゲートウェイのあるインターフェースを検出し、疎通試験で利用可能な上位ネットワーク出口を選びます。
2. 選んだインターフェースから送信するよう、LPMU のデフォルトルートを再設定します。
3. IPv4 転送を有効にし、`iptables` の NAT と FORWARD ルールを設定します。
4. 内部 AGX（既定 `10.10.99.98`）が LPMU（内部 `10.10.99.99`）経由で上位ネットワークにアクセスできるようにします。
5. AGX にアクセスするためのポート転送を設定します。

任意で有効にできるルート監視サービスも含まれます。30 秒ごとに出口を確認し、切断時には別の利用可能なインターフェースへの切り替えを試み、優先インターフェースが復旧すると戻します。

### TianshanOS から実行する

1. C3 が USB-C ↔ RJ45 アダプター経由でスイッチまたはルーターにつながっていることを確認します。
2. ホームページで上部 USB の接続先が `LPMU` になっていることを確認します。
3. 上部ナビゲーションのネットワーク（「网络」）を開きます。
4. 上位ネットワーク接続（「接入上层网络」）を探します。
5. LPMU 経由で接続（「通过LPMU接入」）を選択します。
6. 処理中（「处理中」）から成功（「成功」）または失敗（「失败」）に変わるまで待ちます。処理中に再実行しないでください。

成功は、利用可能な出口を見つけ、LPMU のルートと内部転送・NAT 設定を実行できたことを示します。ボタンの状態だけで判断せず、AGX の IP 疎通と DNS 名前解決も確認することをお勧めします。

### 接続の成功を確認する

まず TianshanOS に成功（「成功」）と表示されることを確認します。AGX のターミナルを使える場合は、AGX 上で順に実行します。

```bash
ping -c 3 10.10.99.99  # AGX から LPMU への疎通
ping -c 3 8.8.8.8       # インターネットの IP 疎通
ping -c 3 google.com    # DNS 名前解決
```

順に内部リンク、インターネット転送、DNS を確認します。最初が失敗したら内部接続やアドレス設定、最初だけ成功したら LPMU の上位接続・デフォルトルート・NAT、最初の 2 つが成功したら DNS 設定を確認してください。

### ポート転送とセキュリティの範囲

現在のプロジェクトは LPMU の上位側インターフェースに次を設定します。

| LPMU の入口 | 転送先 | 用途 |
| --- | --- | --- |
| TCP `58022` | AGX TCP `22` | LPMU 経由で AGX の SSH に接続 |
| TCP/UDP `58000–58999` | AGX の同じポート | AGX のアプリケーションサービスに接続 |

例えば SSH の接続先は LPMU の上位側アドレスですが、ユーザー名と認証情報は AGX のものです。

```bash
ssh -p 58022 <AGX_USERNAME>@<LPMU_IP>
```

> ポート転送は上位ネットワークから AGX のサービスにアクセスできる範囲を広げます。信頼できる LAN だけで使い、スイッチ、ルーター、上位ファイアウォールで送信元を制限してください。これらのポートをインターネットに直接公開しないでください。

### リスクとトラブルシューティング

> 「通过LPMU接入」は LPMU の既存デフォルトルートを削除し、`iptables` の NAT テーブルと FORWARD チェーンを消去し、FORWARD の既定ポリシーを DROP にしてから、RM-01 用のルート・NAT・ポート転送を設定します。リモート接続が即座に切れ、独自のネットワークやファイアウォール設定が上書きされる可能性があります。LPMU をカスタマイズしている場合は、先にルートとファイアウォールのルールをエクスポートし、ローカルで復旧できるようにしてください。

失敗した場合は次の順に確認します。

1. C3 の USB-C ↔ RJ45 アダプターとケーブルが確実に接続されているか。
2. スイッチまたはルーターのポートが有効か。
3. 上部 USB の接続先が `LPMU` と表示されているか。
4. 上位ネットワークが有効なアドレス、ゲートウェイ、DNS を LPMU に提供できるか。
5. 上位ネットワーク接続欄のエラーと出力。
6. AGX から LPMU 内部アドレス `10.10.99.99` に到達できるか。
7. デフォルトルート、NAT、FORWARD、ポート転送と競合する独自ルールが LPMU にないか。
